#include "user.h"
#include <iostream>
#include <fstream>

// Copy all user-related function implementations from the original file
// Including: validateAdminLogin(), clientSignup(), clientLogin()
// Remove the using namespace std; and use std:: prefix
